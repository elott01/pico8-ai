node_modules
dist

# Local env — the Gemini key lives here and must never be committed
.env
.env.local
.env.*.local

# Vercel CLI
.vercel

*.log
# …except bench run logs, which are measurement records rather than stray output. One is
# the only surviving evidence of a run whose JSON was overwritten; see results/README.md.
!bench/results/*.log
.DS_Store
.env*
